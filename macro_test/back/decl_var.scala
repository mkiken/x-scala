Expression Decl {
    Identifier: miss;
    Expression: base;

    { Decl(base, miss) ->
      // [> sss <]
     ((f) => {
       val d = 3;
       if(f) base;
       else d;
     })(base)
    }
}

object Append1{

  val e:Int;
  var f:Int;
  def main(args: Array[String]): Unit = {

    // val e:Int;
    val d = false;
    var c = false;
    val f, h:Int = -1;
    // val f:Int = -1;
    val a = Decl(d, f);
    println($body);
    val g = f;
    var z, y:Int = _;

    }

  val d:Int;
  }

