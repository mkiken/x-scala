Type J {
    Type T;

    { J[T] -> java.lang.T }
}

// import java.lang.Scanner;

object Type2{
  def main(args: Array[String]): Unit = {
    // val ary = makeArray[1 ... 100];
    type c = J[String];
    type d = J[Number];

    val a : Number = 30;
    val n : J[Number] = 30;
    // val n:Int = 30;
    type sc = J[String];
    println(n);
  }
}
