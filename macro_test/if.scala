object Append1{

  def main(args: Array[String]): Unit = {
    // var $body = Append 2 to 3;
    // var $body = Append 2 to 3;
    // var $body = Append {2, 3, 4};
    // println($body);
    println(args(2));
    // if(true) 1 else 0;
    if(flag) if(bOK) "OK" else "NG";
    }
}
